apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app: sample-app
  managed-by: argocd

namespace: default

images:
  - name: sample-app
    newName: sample-app
    newTag: latest
